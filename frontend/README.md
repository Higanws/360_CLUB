# Frontend (React + Vite)

Interfaz Club360. Documentación: **[../docs/desarrollo.md](../docs/desarrollo.md)**.

```bash
npm install
npm run dev      # http://localhost:5173 — proxy /api → :3000
npm run build
npm test
```

Gestión: `/gestion/*`. Portal socio: `/socio/*`.
